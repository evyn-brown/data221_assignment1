file1: create a loop to consecutively multiply integers until first instance where the product is greater than threshold value
prints the integer where the product is greater than threshold, prints the product that is greater than the threshold

file2: define function stringToDictionary(userList=[]) # receive a list of strings and return a dictionary such that:Each key is a string from the list,
Each value is another dictionary containing, The length of the string, Whether the length is even or odd. function will print the new dictionary

file3: given a list of pairs (x,y), creates new function exponentiationOfList(pairs=[]) to compute x^y for each pair on list, computed and stored in a new list

file 4
